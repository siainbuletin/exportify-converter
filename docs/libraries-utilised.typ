the exportify converter uses the `eyeD3` python library in order to parse the metadata in those files.

the library can also be used to write data to a file#footnote[https://eyed3.readthedocs.io/en/latest/]
